mod day1;

fn main() {
    println!(
"I'm actually solving the unique days with the tests, so this one will be kind of empty...
Yes, I should have created it as a lib instead, so no main was required, but I forgot and was to laizy to change it :) "
);
}
